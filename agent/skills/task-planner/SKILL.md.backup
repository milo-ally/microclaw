---
name: task-planner
description: 帮助用户进行复杂任务规划、分解和跟踪的技能。当用户提出复杂需求、多步骤项目或需要系统化管理的任务时使用此技能。使用场景包括：项目规划、任务分解、进度跟踪、资源分配、时间估算等。当用户说"这个任务很复杂"、"需要规划一下"、"帮我分解任务"、"制定项目计划"时，使用此技能。
---

# 任务规划技能

这是一个帮助用户进行复杂任务规划、分解和跟踪的技能。基于Agent Guide中的任务规划协议，提供系统化的任务管理方法。

## 核心原则

1. **任务分解**：将复杂任务分解为可执行的小任务
2. **优先级排序**：为任务设置优先级和依赖关系
3. **进度跟踪**：实时跟踪任务完成状态
4. **资源管理**：估算时间和资源需求
5. **灵活调整**：根据实际情况调整计划

## 使用场景

当遇到以下情况时，使用此技能：
- 用户提出复杂或多步骤的需求
- 需要系统化管理的项目
- 需要跟踪进度和状态的任务
- 需要估算时间和资源的工作
- 需要明确责任和交付物的项目

## 工作流程

### 1. 任务分析阶段
- 理解用户的核心需求
- 识别关键目标和约束条件
- 确定成功标准
- 收集必要的信息和资源

### 2. 任务分解阶段
- 将大任务分解为小任务
- 识别任务间的依赖关系
- 估算每个任务的时间和资源
- 设置优先级（高/中/低）

### 3. 规划文档创建
- 创建 `tasks.json` 文件记录所有任务
- 每个任务包含：ID、描述、状态、优先级、依赖、估算时间
- 创建任务看板或甘特图（可选）
- 生成项目概览文档

### 4. 执行与跟踪阶段
- 按计划执行任务
- 定期更新任务状态
- 记录完成情况和遇到的问题
- 根据需要调整计划

### 5. 总结与复盘阶段
- 汇总项目成果
- 分析执行过程中的问题
- 总结经验教训
- 归档项目文档

## 任务数据结构

在 `tasks.json` 文件中使用以下结构：

```json
{
  "project_name": "项目名称",
  "created_at": "创建时间",
  "last_updated": "最后更新时间",
  "total_tasks": 0,
  "completed_tasks": 0,
  "tasks": [
    {
      "id": "task_1",
      "description": "任务描述",
      "status": "pending|in_progress|completed|blocked",
      "priority": "high|medium|low",
      "dependencies": ["task_2", "task_3"],
      "estimated_time": "2h",
      "actual_time": "",
      "assigned_to": "",
      "notes": "",
      "created_at": "创建时间",
      "completed_at": ""
    }
  ]
}
```

## 工具和命令

### 创建任务规划
```bash
# 创建tasks.json文件
echo '{"project_name": "", "tasks": []}' > tasks.json
```

### 更新任务状态
```bash
# 使用python_repl更新任务状态
python3 -c "
import json
with open('tasks.json', 'r') as f:
    data = json.load(f)
# 更新逻辑
with open('tasks.json', 'w') as f:
    json.dump(data, f, indent=2, ensure_ascii=False)
"
```

### 生成进度报告
```bash
# 生成文本报告
python3 -c "
import json
from datetime import datetime
with open('tasks.json', 'r') as f:
    data = json.load(f)
    
completed = sum(1 for t in data['tasks'] if t['status'] == 'completed')
total = len(data['tasks'])
progress = (completed/total*100) if total > 0 else 0

print(f'项目: {data[\"project_name\"]}')
print(f'进度: {completed}/{total} ({progress:.1f}%)')
print(f'最后更新: {data[\"last_updated\"]}')
print('\\n任务状态:')
for task in data['tasks']:
    print(f'  [{task[\"status\"][0].upper()}] {task[\"id\"]}: {task[\"description\"]}')
"
```

## 示例用例

### 示例1：网站开发项目
```
用户：我需要开发一个电商网站，包含用户注册、商品展示、购物车和支付功能。

技能响应：
1. 分析需求，识别核心功能模块
2. 创建tasks.json，分解为：
   - task_1: 需求分析和原型设计
   - task_2: 数据库设计和搭建
   - task_3: 用户认证系统开发
   - task_4: 商品管理模块开发
   - task_5: 购物车功能开发
   - task_6: 支付接口集成
   - task_7: 前端界面开发
   - task_8: 测试和部署
3. 设置依赖关系（如task_3依赖task_2）
4. 开始执行第一个任务
```

### 示例2：数据分析报告
```
用户：我需要分析销售数据并生成季度报告。

技能响应：
1. 创建任务规划：
   - task_1: 数据收集和清洗
   - task_2: 数据探索性分析
   - task_3: 关键指标计算
   - task_4: 可视化图表制作
   - task_5: 报告文档撰写
   - task_6: 报告审阅和修改
2. 估算每个任务时间
3. 按顺序执行任务并更新状态
```

## 最佳实践

1. **保持任务粒度适中**：每个任务应该在2-8小时内完成
2. **明确验收标准**：每个任务应该有明确的完成标准
3. **定期更新状态**：每完成一个任务就更新tasks.json
4. **灵活调整**：根据实际情况调整任务优先级和计划
5. **文档化决策**：记录重要的决策和变更原因

## 与其他技能的集成

- 与 **docx** 技能集成：生成正式的项目计划文档
- 与 **xlsx** 技能集成：创建任务跟踪表格
- 与 **pptx** 技能集成：制作项目汇报幻灯片
- 与 **theme-factory** 技能集成：美化项目文档

## 故障排除

### 问题：任务过于复杂
**解决方案**：进一步分解任务，确保每个任务都有明确的产出

### 问题：依赖关系混乱
**解决方案**：重新梳理任务依赖，使用甘特图可视化

### 问题：进度滞后
**解决方案**：重新评估任务优先级，调整资源分配

### 问题：需求变更
**解决方案**：更新tasks.json，记录变更原因，调整后续任务

## 模板文件

在 `./skills/task-planner/templates/` 目录中提供以下模板：
- `project_plan_template.md` - 项目计划文档模板
- `task_board_template.html` - 任务看板HTML模板
- `progress_report_template.docx` - 进度报告Word模板

## 注意事项

1. 始终从用户需求出发，不要过度规划
2. 保持规划文档的简洁性和实用性
3. 定期与用户确认进度和方向
4. 根据实际情况灵活调整计划
5. 确保任务分解后的每个子任务都是可执行和可验证的

---

**记住**：好的任务规划是成功的一半。使用此技能可以帮助用户系统化地管理复杂项目，提高工作效率和成功率。
